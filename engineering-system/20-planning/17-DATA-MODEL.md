# Data Model

As appropriate document entities/ownership, attributes/types, relationships, constraints, indexes/access patterns, lifecycle/retention, sensitive fields, migration strategy, archival/deletion and consistency needs.

Do not create complex ERDs for projects that do not need them.
