# Environment & Reproducibility

Record as applicable:
- OS/runtime versions
- compiler/interpreter versions
- package manager and lockfiles
- dependency versions/hashes
- required services
- environment variable names, never secret values
- migrations/seeds
- build/test commands
- container/toolchain versions
- external sandbox/emulator requirements

A successful build on one agent machine is not proof of reproducibility.
